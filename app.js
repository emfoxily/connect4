$(()=> {
  // call the function that will prompt the user to pick a house
  selectHouse();
  // call the function that will make the sorting hat appear and say who goes first

  // call function that will create the game board
  createBoard();

})
